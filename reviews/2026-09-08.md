## WHERE WE ARE
The destination is: Debrief runs nightly from its own repo: every morning a blunt 10–12-task list lands by separate email, sized in fixed 2-hour tiers, with reports committed as history.

The remaining open issues are:
- Prove the first live run
- Push-capable token for watched repos

We are in the initial setup phase. The core code and Map plan have just been committed into this repository, but no end-to-end live run has been executed or proven yet.

Plan changes: The plan itself was created today in commit `9bded9f`, adding `.scratch/launch/map.md` and initial seed issues for tracking launch progress.

## WHAT HAPPENED TODAY
Luke established this repository as the home for Debrief. He pushed the workflow automation (`.github/workflows/project-review.yml`), core reviewer modules (`briefing.py`, `daily_brief.py`, `llm.py`), execution scripts (`scripts/review_project.py`, `scripts/pace.py`), unit tests, and the seed map setup (`.scratch/launch/map.md`). He also committed `marketdata.py` and `verify.py`.

## ON TRACK OR NOT
On track for setting up the standalone repo, but dragging in extra code. Setting up GitHub Actions, test scripts, and the launch map directly supports the destination. However, dumping `marketdata.py` (193 lines of market/financial data code) and `verify.py` into this repo is sideways work unless they are strictly required for email dispatch. The focus must remain strictly on executing a real run, emailing the output, committing the history, and setting up cross-repo access.

## TOMORROW

### FIRST 2 HOURS
1. `.github/workflows/project-review.yml`: Audit environment variables and secrets (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`) to confirm email sending credentials are set up for live runs.
2. `daily_brief.py`: Check the email generation logic to ensure Markdown outputs from `review_project.py` format cleanly in email bodies.
3. `scripts/review_project.py`: Verify the git commit step that writes completed daily report markdown files back into repository history.

### NEXT 2 HOURS
4. `scripts/review_project.py`: Add output validation ensuring generated reports contain all required headings before sending email notifications.
5. `.github/workflows/project-review.yml`: Ensure a `workflow_dispatch` trigger exists to allow manual test runs without waiting for midnight schedule.
6. `.scratch/launch/issues/01-first-live-run.md`: Update acceptance criteria to define exact success conditions (email landed + report file committed).

### THIRD 2 HOURS
7. `.github/workflows/project-review.yml`: Add `DEBRIEF_GITHUB_TOKEN` secret binding for authenticating cross-repo checkouts.
8. `scripts/review_project.py`: Update repo target fetching to use `DEBRIEF_GITHUB_TOKEN` when interacting with watched repositories.
9. `scripts/review_project.py`: Implement explicit failure handling and logging if repository cloning or token validation fails.

### FOURTH 2 HOURS
10. `scripts/test_review_map_plan.py`: Add unit test coverage verifying map parsing behavior when inspecting external watched repositories.
11. `.scratch/launch/issues/02-cross-repo-token.md`: Document required GitHub PAT scope permissions for multi-repo inspection and updates.
